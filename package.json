{
    "name": "market-tracker-root",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "npm run install-client && npm run install-server && npm run install-chrome",
        "install-client": "cd client && npm install",
        "install-server": "cd server && npm install",
        "build": "cd client && npm run build",
        "build:all": "npm run build && echo 'Client build completed'",
        "start": "cd server && node server.js"
    },
    "engines": {
        "node": "20.x"
    }
}