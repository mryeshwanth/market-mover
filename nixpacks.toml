[phases.setup]
nixPkgs = ['nodejs-20_x', 'chromium']

[phases.install]
cmds = [
  'npm install',
  'cd server && npx puppeteer browsers install chrome || echo "Chrome install skipped"'
]

[phases.build]
cmds = [
  'cd client && npm run build || echo "Build skipped"'
]

[start]
cmd = 'npm start'

